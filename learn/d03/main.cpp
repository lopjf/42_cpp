#include "sample.class.hpp"
#include <iostream>

int main()
{
	Sample instance;

	std::cout << "hello\n";

	// the destructor is called automatically when the variable is destroyed (out of scope)
	return (0);
}